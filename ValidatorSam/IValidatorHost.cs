namespace ValidatorSam
{
    /// <summary>
    /// Implement this interface so that when method <see cref="Validator.GetAll(object)"/> is called, 
    /// it includes this object in the search for retrieving the 
    /// list of validators
    /// </summary>
    public interface IValidatorHost
    {
    }
}